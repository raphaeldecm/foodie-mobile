import { COLORS } from "../../constants/theme";

export const styles = {
  container: {
    flex: 1,
    padding: 40,
    backgroundColor: COLORS.white,
    justifyContent: "center",
    alignItems: "center"
  }
};
