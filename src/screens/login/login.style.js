import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    form: {
        width: "100%",
        marginBottom: 25
    },
    formGroup: {
        width: "100%",
        marginTop: 50,
        marginBottom: 40
    },
}