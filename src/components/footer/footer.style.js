import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
  footer: {
    width: "100%",
    position: "absolute",
    bottom: 0,
    marginBottom: 40
  },
  footerText: {
    textAlign: "center",
    color: COLORS.dark_gray,
    fontSize: FONT_SIZE.md
  }
}