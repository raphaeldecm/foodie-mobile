import { COLORS, FONT_SIZE } from "../../constants/theme";

export const styles = {
  btn: {
    backgroundColor: COLORS.red,
    width: '100%',
    borderRadius: 8,
  },
  text: {
    color: 'white',
    fontSize: FONT_SIZE.md,
    padding: 14,
    textAlign: 'center',
  }
};
